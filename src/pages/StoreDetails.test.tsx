import { render } from "@testing-library/react";
import StoreDetails from "./StoreDetails";

describe("StoreDetails", () => {
  it("renders StoreDetails Component", () => {
    render(<StoreDetails />);
  });
});
